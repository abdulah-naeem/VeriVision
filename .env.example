# VeriVision Environment Configuration
# Copy this file to .env and fill in your actual connection string

# Get this from MongoDB Atlas -> Connect -> Drivers -> Python
# NOTE: If your password contains special characters like @, :, or #, they MUST be URL-encoded.
MONGO_URI="your mongodb string"
